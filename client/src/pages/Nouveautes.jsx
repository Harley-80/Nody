// Importe React pour définir le composant.
import React from 'react'

// Définit le composant fonctionnel "Nouveautes".
const Nouveautes = () => {
    // Le composant retourne une structure HTML simple pour l'instant.
    return (
        <div>
            <h1>Nouveautés</h1>
            <p>Découvrez les derniers produits ajoutés à notre collection !</p>
        </div>
    )
}

// Exporte le composant "Nouveautes" par défaut pour qu'il soit utilisé par le routeur.
export default Nouveautes
